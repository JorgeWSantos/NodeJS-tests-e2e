import { jest, expect, it, describe } from "@jest/globals";

describe("API E2E Test Suite", () => {
  it("should return an array / GET", () => {});

  it("should save an item and return ok / POST", () => {});

  it("should be able to delete all data / DELETE", () => {});
});
